# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=posiflora
DB_NAME=posiflora
DB_SSLMODE=disable

# Server
SERVER_PORT=8080
SERVER_HOST=0.0.0.0

# Telegram (для мок-режима можно оставить true)
TELEGRAM_MOCK_MODE=true

# Debug mode (для подробного логирования SQL запросов)
DEBUG=false

# CORS (разрешенные источники, через запятую)
CORS_ORIGINS=http://localhost:5173
